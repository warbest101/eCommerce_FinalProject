@model IEnumerable<WebBanHang.Models.OderDetail>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>xemdonhang</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Gia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaHH)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Gia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaHH)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OderID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OderID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OderID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
